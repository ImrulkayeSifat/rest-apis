let DEBUG_MODE,ValidationError,CustomErrorHandler;_575‍.x([["default",()=>_575‍.o]]);_575‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_575‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);_575‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);



const errorHandler = (err,req,res,next) => {
  let statusCode = 500;

  let data = {
    message: 'Internal server error',
    ...DEBUG_MODE(DEBUG_MODE === 'true' && { originalError: err.message })
  }

  if(err instanceof ValidationError) {
    statusCode = 422;
    data={
      message:err.message
    }
  }

  if(err instanceof CustomErrorHandler){
    statusCode = err.status;
    data={
      message:err.message
    }
  }

  return res.status().json(data);
}

_575‍.d(errorHandler);